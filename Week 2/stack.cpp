#include "stack.h"

#include <iostream>
#include <vector>
using namespace std;
using ll = long long;
using vint = vector<int>;

void vprint(const vint& v) {
    for (const int& i : v) { cout << i << " "; }
    cout << "\n";
}

int main() {}
