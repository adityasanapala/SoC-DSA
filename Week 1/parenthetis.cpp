#include <iostream>
#include <vector>
using namespace std;
using vint = vector<int>;

void vprint(const vint& v) {
    for (const int& i : v) { cout << i << " "; }
    cout << "\n";
}

// I want an array of all the possible numbers
int main() {}
